-- Development seed data used to live here (Ocean Mall location plus a placeholder
-- admin profile that was not tied to an auth user). Seed data now lives in
-- supabase/seed.sql and is only applied to local/dev databases. This file is kept
-- as a no-op so the migration history stays consistent across environments.
SELECT 1;
